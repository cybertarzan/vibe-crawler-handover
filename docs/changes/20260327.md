# 📦 2026-03-27 배포

> **커밋**: `f1884694`, `2dc39b19`

---

## 변경 요약

- 프로젝트 `brand_name`/`brand_id` 조회 시 `null` 반환되던 버그 수정
- 프로젝트 이름 파싱 순서 수정 (브랜드명 위치 오류)
- 광고주/브랜드 드롭다운 무한 스크롤 페이징 (200건 단위)
- 카테고리 선택 검색 필터링 추가
- **[Critical] 전역 싱글턴 패턴 완전 삭제**로 다중 API 요청 간 DB 세션 겹침(동시성 에러) 근본 해결
- DB 에러 시 `session.rollback()` 처리 보강을 통한 `InFailedSQLTransactionError` 500 예외 방지
- 전역 Repository 메모리 오염 차단 매크로 단위 테스트(`test_global_repository_isolation`) 도입

## 수정 파일

**Backend**

- `project_repository_adapter.py` 등 15개 Repository — 레거시 싱글턴 캐싱 로직(`_init_done`) 완전 삭제
- 백엔드 서비스 객체 약 30곳 — 빈 생성자 에러 복구(`pass` 주입) 및 비정상 싱글턴 패턴 소거
- `test_global_repository_isolation.py` [NEW] — 15개 Repository 대상 세션 겹침 검사 강력한 자동 단위 테스트
- `test_db_concurrency_isolation.py` [NEW] — 동시성 제어 및 `rollback` 무결성 검증용
- DB 변환 및 DTO (`update_project_service`, `get_project_service`, `controllers`) — `brand` 필드 매핑 로직 추가
- `database.py` — 전역 DB 예외 롤백 보완

**Frontend**

- `ProjectDetailPage.vue` — 이름 파싱 순서 수정
- `HybridSelect.vue` — 무한 스크롤 페이징
- `CategorySelect.vue` — 검색 필터링

## DB 스키마

!!! note "참고"
    컬럼은 기존 배포에서 추가 완료. 이번 배포는 매핑 수정만 해당.

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `brand_name` | `VARCHAR(100)` | 브랜드명 |
| `brand_id` | `VARCHAR(50)` | 브랜드 코드 |
