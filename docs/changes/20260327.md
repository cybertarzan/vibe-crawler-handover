# 📦 2026-03-27 배포

> **커밋**: `f1884694`, `2dc39b19`

---

## 변경 요약

- 프로젝트 `brand_name`/`brand_id` 조회 시 `null` 반환되던 버그 수정
- 프로젝트 이름 파싱 순서 수정 (브랜드명 위치 오류)
- 광고주/브랜드 드롭다운 무한 스크롤 페이징 (200건 단위)
- 카테고리 선택 검색 필터링 추가

## 수정 파일

**Backend**

- `project_repository_adapter.py` — DB→엔티티 변환 6곳에 brand 매핑 추가
- `update_project_service.py` — brand 파라미터/로직 추가
- `get_project_service.py` — 상세 조회 DTO에 brand 추가
- `get_projects_service.py` — 목록 조회 DTO에 brand 추가
- `project_controller.py` — 생성/수정 응답에 brand 추가

**Frontend**

- `ProjectDetailPage.vue` — 이름 파싱 순서 수정
- `HybridSelect.vue` — 무한 스크롤 페이징
- `CategorySelect.vue` — 검색 필터링

## DB 스키마

!!! note "참고"
    컬럼은 기존 배포에서 추가 완료. 이번 배포는 매핑 수정만 해당.

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `brand_name` | `VARCHAR(100)` | 브랜드명 |
| `brand_id` | `VARCHAR(50)` | 브랜드 코드 |
